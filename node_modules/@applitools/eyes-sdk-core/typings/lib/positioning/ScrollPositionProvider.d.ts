export = ScrollPositionProvider;
declare const ScrollPositionProvider_base: typeof import("./PositionProvider");
/**
 * @typedef {import('../geometry/RectangleSize')} RectangleSize
 */
/**
 * @template TDriver, TElement, TSelector
 * @typedef {import('../wrappers/EyesJsExecutor')<TDriver, TElement, TSelector>} EyesJsExecutor
 */
/**
 * @template TDriver, TElement, TSelector
 * @typedef {import('../wrappers/EyesWrappedElement')<TDriver, TElement, TSelector>} EyesWrappedElement
 */
/**
 * A {@link PositionProvider} which is based on Scroll
 *
 * @internal
 * @template TDriver
 * @template TElement
 * @template TSelector
 */
declare class ScrollPositionProvider<TDriver, TElement, TSelector> extends ScrollPositionProvider_base {
    /**
     * @param {Logger} logger - logger instance
     * @param {EyesJsExecutor<TDriver, TElement, TSelector>} executor - js executor
     * @param {EyesWrappedElement<TDriver, TElement, TSelector>} [scrollRootElement] - if scrolling element is not provided, default scrolling element will be used
     */
    constructor(logger: any, executor: import("../wrappers/EyesJsExecutor")<TDriver, TElement, TSelector>, scrollRootElement?: import("../wrappers/EyesWrappedElement")<TDriver, TElement, TSelector>);
    _logger: any;
    _executor: import("../wrappers/EyesJsExecutor")<TDriver, TElement, TSelector>;
    _scrollRootElement: import("../wrappers/EyesWrappedElement")<TDriver, TElement, TSelector>;
    /**
     * @type {EyesWrappedElement<TDriver, TElement, TSelector>}
     */
    get scrollRootElement(): import("../wrappers/EyesWrappedElement")<TDriver, TElement, TSelector>;
    /**
     * Add "data-applitools-scroll" attribute to the scrolling element
     */
    markScrollRootElement(): Promise<void>;
}
declare namespace ScrollPositionProvider {
    export { RectangleSize, EyesJsExecutor, EyesWrappedElement };
}
type EyesJsExecutor<TDriver, TElement, TSelector> = import("../wrappers/EyesJsExecutor")<TDriver, TElement, TSelector>;
type EyesWrappedElement<TDriver, TElement, TSelector> = import("../wrappers/EyesWrappedElement")<TDriver, TElement, TSelector>;
type RectangleSize = import("../geometry/RectangleSize");
