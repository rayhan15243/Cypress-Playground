export = ImageRotation;
/**
 * Encapsulates rotation data for images.
 */
declare class ImageRotation {
    /**
     * @param {int} rotation The degrees by which to rotate.
     */
    constructor(rotation: any);
    _rotation: any;
    /**
     * @return {int} The degrees by which to rotate.
     */
    getRotation(): any;
}
